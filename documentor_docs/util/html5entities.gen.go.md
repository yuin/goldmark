# Technical Documentation: `util/html5entities.gen.go`

## Overview

The `util/html5entities.gen.go` file is an auto-generated Go source file within the `util` package. It provides low-level data tables mapping HTML5 entity names (e.g., named character references such as `&AMP;` or `&AElig;`) to their corresponding UTF-8 byte sequences.

> **Note:** As indicated by the header comment (`// Code generated by _tools; DO NOT EDIT.`), this file is generated automatically by toolchain scripts located in `_tools` and should not be modified manually.

---

## Package Declaration

```go
package util
```

The file belongs to the `util` package, providing utility-level lookup data for HTML parsing, decoding, or escaping routines.

---

## Constants and Package Variables

The file defines one constant and four unexported package-level variables designed to store compressed entity name and byte mapping tables.

### 1. `_html5entitiesLength`

```go
const _html5entitiesLength = 2124
```

* **Type:** Untyped Integer Constant
* **Value:** `2124`
* **Description:** Represents the total number of HTML5 named entities indexed across the mapping arrays.

---

### 2. `_html5entitiesName`

```go
const _html5entitiesName string = "AEligAMPAacuteAcirc..."
```

* **Type:** `string`
* **Description:** A single concatenated string containing all 2,124 HTML5 entity names (without leading `&` or trailing `;`). 
* **Design Purpose:** Concatenating all entity names into a single string literal minimizes binary overhead and avoids memory allocation costs associated with holding a slice of individual strings (`[]string`).

---

### 3. `_html5entitiesNameIndex`

```go
var _html5entitiesNameIndex string = "\x05\x03\x06..."
```

* **Type:** `string`
* **Description:** A byte-encoded string where each byte character represents the length of the corresponding entity name in `_html5entitiesName`.
* **Usage:** For example, if the first byte is `\x05`, the first entity name spans 5 bytes in `_html5entitiesName` (`AElig`). The second byte `\x03` indicates the next entity name spans 3 bytes (`AMP`), and so on.

---

### 4. `_html5entitiesCharacters`

```go
var _html5entitiesCharacters = [...]byte{0xc3, 0x86, 0x26, ...}
```

* **Type:** `[N]byte` (Fixed-size byte array)
* **Description:** A contiguous array of UTF-8 encoded bytes representing the target characters corresponding to each entity.
* **Examples from code:**
  * `0xc3, 0x86` is the UTF-8 sequence for `Æ` (`&AElig;`).
  * `0x26` is the ASCII byte for `&` (`&AMP;`).

---

### 5. `_html5entitiesCharactersIndex`

```go
var _html5entitiesCharactersIndex string = "\x02\x01\x02..."
```

* **Type:** `string`
* **Description:** A byte-encoded string where each byte value specifies the length (number of bytes) of the UTF-8 character sequence stored in `_html5entitiesCharacters` for the corresponding entity.
* **Usage:** Paired with `_html5entitiesCharacters`, this allows extracting the exact target byte slice for an entity at index `i`.

---

## Data Architecture & Memory Strategy

The layout of `html5entities.gen.go` is specifically designed for high-performance string lookups and zero-allocation runtime reads:

1. **Flat Data Storage:** Rather than storing maps (`map[string]string`) or slices of structures (`[]struct{ Name, Value string }`), the generator flattens all string keys and byte values into contiguous memory.
2. **Reduced GC Overhead:** Storing data as primitive arrays and string constants prevents the Go Garbage Collector (GC) from scanning thousands of individual string headers or map pointers.
3. **Index Slicing:** By using cumulative offset calculations derived from `_html5entitiesNameIndex` and `_html5entitiesCharactersIndex`, caller functions in the `util` package can quickly resolve entity names and their matching UTF-8 byte representations without dynamic memory allocation.

---

## Summary Table

| Identification | Type | Size / Length | Role |
| :--- | :--- | :--- | :--- |
| `_html5entitiesLength` | `const` | 2124 | Total count of supported HTML5 entities |
| `_html5entitiesName` | `const string` | Packed String | Concatenated entity names |
| `_html5entitiesNameIndex` | `var string` | 2124 bytes | Length of each entity name entry |
| `_html5entitiesCharacters` | `var [...]byte` | Packed Bytes | Contiguous UTF-8 encoded character bytes |
| `_html5entitiesCharactersIndex` | `var string` | 2124 bytes | Byte count for each mapped UTF-8 character |