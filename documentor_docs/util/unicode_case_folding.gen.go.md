# Technical Documentation: `util/unicode_case_folding.gen.go`

## Overview

The `util/unicode_case_folding.gen.go` file contains auto-generated static lookup tables for performing **Unicode case-folding** operations within the `util` package. 

> **Important:** As indicated by the top-level comment (`// Code generated by _tools; DO NOT EDIT.`), this file is generated automatically by scripts in the `_tools` directory. Manual edits to this file should not be made directly, as they will be overwritten during future code generation runs.

---

## Constants

### `_unicodeCaseFoldingLength`

```go
const _unicodeCaseFoldingLength = 1530
```

* **Type:** Untyped integer constant
* **Value:** `1530`
* **Purpose:** Represents the total number of distinct source code points (`runes`) mapped in the case-folding table (`_unicodeCaseFoldingFrom`).

---

## Data Structures & Variables

The file defines three internal, unexported data structures that collectively encode Unicode case-folding mappings.

### 1. `_unicodeCaseFoldingFrom`

```go
var _unicodeCaseFoldingFrom = [...]rune{ ... }
```

* **Type:** Fixed-size array of `rune` (size implicitly set to `1530`)
* **Purpose:** Stores the sorted list of source Unicode code points (`runes`) that require specific case-folding transformations.
* **Content:** Contains hex-encoded Unicode code point values ranging from standard Latin characters (e.g., `0x41` for `'A'`) to higher Unicode blocks (Greek, Cyrillic, Georgian, Armenian, Cherokee, full-width characters, etc.).

### 2. `_unicodeCaseFoldingTo`

```go
var _unicodeCaseFoldingTo = [...]rune{ ... }
```

* **Type:** Fixed-size array of `rune`
* **Purpose:** Stores the target replacement `rune` sequences resulting from case-folding the corresponding characters in `_unicodeCaseFoldingFrom`.
* **Content:** Contains decimal-encoded `rune` values representing folded equivalents. Since a single Unicode character can fold into one or multiple target characters (such as ligatures or special Greek/Latin letters), `_unicodeCaseFoldingTo` may contain contiguous sequences of replacement characters.

### 3. `_unicodeCaseFoldingToIndex`

```go
var _unicodeCaseFoldingToIndex = "..."
```

* **Type:** `string` (used as a byte array literal)
* **Purpose:** Encodes metadata regarding the number of target runes mapped to each source rune in `_unicodeCaseFoldingFrom`.
* **Content:** Formatted as a raw byte string containing byte values (e.g., `\x01`, `\x02`, `\x03`):
  * `\x01`: Indicates a 1-to-1 character mapping.
  * `\x02`: Indicates a 1-to-2 character mapping (e.g., a single character folding into two target runes).
  * `\x03`: Indicates a 1-to-3 character mapping.

---

## How the Lookup Data Works

The three components work together to provide complete Unicode case-folding mappings:

1. **Source Lookup (`_unicodeCaseFoldingFrom`):** A target input `rune` is matched against the sorted `_unicodeCaseFoldingFrom` array (typically using binary search due to sorted order).
2. **Target Length Determination (`_unicodeCaseFoldingToIndex`):** The index of the matched rune in `_unicodeCaseFoldingFrom` corresponds to the same byte offset in `_unicodeCaseFoldingToIndex`. The byte value at this index indicates how many runes in `_unicodeCaseFoldingTo` constitute the target folded result.
3. **Target Extraction (`_unicodeCaseFoldingTo`):** Using a calculated offset based on prior index lengths, the mapping function extracts the designated slice of runes from `_unicodeCaseFoldingTo`.

---

## Maintenance Notes

* **Do Not Edit Directly:** Any updates to the Unicode standard or folding tables must be made by updating the generation tool located in `_tools` and re-running the generation process.
* **Package Scope:** All constants and variables in this file are unexported (prefixed with `_`) and are intended strictly for internal use by functions within the `util` package.