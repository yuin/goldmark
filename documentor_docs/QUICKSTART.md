Based on the provided code snippets, there is **insufficient information** to create a complete `QUICKSTART.md` guide. The snippets contain internal Go code structures and utility functions, but do not contain:

* Installation commands or instructions (e.g., `go get` commands)
* Prerequisites or required dependencies
* Environment variables
* Setup configuration steps or runnable usage examples

If you can provide additional context or snippets covering installation, package exports, or environment configuration, a `QUICKSTART.md` can be generated.